/**
*  This example demonstrates how data in the Message can be manipulated
*
*  In this example we go through setting up the message and 
*  preparing it for transmission via the Lithne library.
*
*  (c) Remco Magielse, Intelligent Lighting Institute, 2013
**/

import ili.lithne.*;  //Import the Lithne library

/**
*  The first thing to do is to create a new Message object.
*  We create a new Message object with the name demoMessage
**/
Message demoMessage  =  new Message();

void setup()
{
  /**
  *  The first step in setting up the message
  *  
  **/
  demoMessage.setScope( "demo" );
  demoMessage.setFunction( 123 );
  
  demoMessage.addArgument( 45 );
  demoMessage.addArgument( 67 );
  demoMessage.addArgument( 89 );
  
  println("1. "+demoMessage.toString() );
  
  demoMessage.setStringArgument( "demomessage" );
  
  println("2. "+demoMessage.toString() );
}

void draw()
{
  
}

